[build]
  command = """
    pip install wheel setuptools &&
    pip install -r requirements.txt &&
    python -m spacy download en_core_web_sm
  """
  publish = "build"

[build.environment]
  PYTHON_VERSION = "3.10"
